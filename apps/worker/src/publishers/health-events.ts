// Redis pub/sub domain event publisher — implemented in Story 4.5
// Publishes: evidence.collected, control.degraded, integration.failed, report.generated
// Channel pattern: tenant:{tenantId}:control-health

export {};
