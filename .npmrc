shamefully-hoist=false
strict-peer-dependencies=false

# Allow build scripts for required native packages
# These are reviewed and trusted:
#   - prisma: ORM client generation (packages/db)
#   - @prisma/client: Prisma client
#   - @prisma/engines: Prisma engine binaries
#   - esbuild: bundler used by tsx/vitest
